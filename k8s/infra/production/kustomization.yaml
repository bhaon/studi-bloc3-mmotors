apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - namespace.yaml
  - resource-quota.yaml
  - network-policy.yaml
  - secrets.yaml
  - rbac.yaml
  - hpa.yaml
  - pdb.yaml