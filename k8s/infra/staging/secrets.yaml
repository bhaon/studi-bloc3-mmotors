---
# ============================================================
# SECRETS — STAGING (valeurs placeholder)
# À adapter avant usage en environnement réel.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # Doit être fourni au backend (JWT). Mettre une valeur >= 32 caractères.
  SECRET_KEY: "CHANGE_ME_STAGING_SECRET_KEY_AT_LEAST_32_CHARS"
---
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  POSTGRES_DB: "myapp"
  POSTGRES_USER: "myapp_user"
  POSTGRES_PASSWORD: "rsjxEjf4KBDm7SDxsZ6m4tuJL+mBChf1165m69FX+YE="
  # Utilisé par le backend + initContainer db-migrate
---
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
type: Opaque
stringData:
  # Mot de passe Redis utilisé par le service Redis et le backend.
  REDIS_PASSWORD: "CHANGE_ME_REDIS_PASSWORD"
