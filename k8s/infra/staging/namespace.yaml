---
# ============================================================
# NAMESPACE — dev
# Label requis pour les NetworkPolicy namespaceSelector
# ============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: staging
  labels:
    kubernetes.io/metadata.name: staging
    environment: staging
