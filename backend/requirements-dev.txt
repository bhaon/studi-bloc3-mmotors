# Dépendances d’exécution + pin bcrypt (passlib 1.7.4 incompatible avec bcrypt 4.x)
-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
pytest-timeout==2.3.1
ruff==0.8.4
mypy==1.13.0
bandit[toml]==1.7.10
